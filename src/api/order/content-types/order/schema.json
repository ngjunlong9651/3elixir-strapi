{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "order",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "customerName": {
      "type": "string"
    },
    "customerContact": {
      "type": "string"
    },
    "customerAddress": {
      "type": "string"
    },
    "remarks": {
      "type": "string"
    },
    "orderCollectionDateTime": {
      "type": "datetime"
    },
    "orderStatus": {
      "type": "enumeration",
      "enum": [
        "Pending",
        "Processing",
        "Completed",
        "Cancelled"
      ]
    },
    "paymentMethod": {
      "type": "enumeration",
      "enum": [
        "Cash",
        "PayNow",
        "PayLah",
        "Credit Card"
      ]
    },
    "salesChannel": {
      "type": "enumeration",
      "enum": [
        "walk-in",
        "amazon",
        "carousell",
        "website",
        "b2b",
        "b2c"
      ]
    },
    "orderId": {
      "type": "string"
    },
    "orderProducts": {
      "type": "json"
    },
    "salesAgent": {
      "type": "enumeration",
      "enum": [
        "Agent A",
        "Agent B",
        "Agent C",
        "Agent D"
      ]
    }
  }
}
